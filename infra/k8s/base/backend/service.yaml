# Backend API Service
apiVersion: v1
kind: Service
metadata:
  name: igolo-backend
  labels:
    app: igolo-backend
    component: api
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
  selector:
    app: igolo-backend
